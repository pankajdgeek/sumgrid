package org.dgeek.sumgrid.engine

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder test — verifies JVM test harness is wired correctly.
 *
 * Real engine tests are added in Sprint S01 (T002 — xorshift128 PRNG,
 * T003 — puzzle generator, T004 — uniqueness validator).
 */
class PlaceholderTest {

    @Test
    fun scaffoldIsWiredCorrectly() {
        assertEquals(4, 2 + 2)
    }
}
